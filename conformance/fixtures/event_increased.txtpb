# A single prior Increased event page (orthogonal scaffold). The step
# definition replays it N times with consecutive sequences to build the
# "already recorded N increases" history.
event {
  [type.googleapis.com/test.counter.Increased] {}
}
